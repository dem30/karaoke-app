<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.RECORD_AUDIO"/>
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
<uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
<uses-permission android:name="android.permission.WAKE_LOCK"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
<uses-permission android:name="android.permission.VIBRATE"/>

<!-- ✅ MOI (fix returnToSourceApp() tra ve null tren Android 11+): tu API 30,
     PackageManager AN package khac khoi app minh theo mac dinh (package
     visibility) - getLaunchIntentForPackage()/getPackageInfo() se tra ve
     null/exception voi BAT KY package nao KHONG duoc khai bao o day, DU
     package do co cai that su tren may. Day la ly do returnToSourceApp()
     luon that bai.

     ⚠️ QUAN TRONG: PHAI khai bao CA HAI package trong TARGET_APP_CANDIDATES
     (xem PlaybackCaptureService.kt), KHONG chi package cuoi cung duoc nhac
     toi trong log loi. Lan sua truoc CHI khai bao "com.google.android.youtube"
     (candidate uu tien 2/fallback) ma quen "com.android.chrome" (candidate
     uu tien 1 - noi video THAT SU dang phat, theo dung ghi chu trong
     PlaybackCaptureService.kt). Hau qua: Chrome van bi an (query tra ve
     null), code roi xuong candidate 2 va mo NHAM app YouTube goc (phien
     rong, khong phat gi) thay vi quay lai tab Chrome dang phat that -> tuong
     nhu "da nhay sang YouTube" nhung khong co tieng/tieng nho vi do la SAI
     nguon am thanh. Neu sau nay them candidate moi vao danh sach do (vi du
     Firefox, Samsung Internet...), PHAI them dong <package> tuong ung o day
     - thieu 1 candidate nao trong danh sach se lam candidate do bi PackageManager
     coi nhu "khong ton tai" du may co cai that. -->
<queries>
    <package android:name="com.android.chrome" />
    <package android:name="com.google.android.youtube" />
</queries>

<application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name" android:theme="@style/Theme.KaraokeApp" android:supportsRtl="true">
<activity android:name=".MainActivity" android:exported="true">
<intent-filter>
<action android:name="android.intent.action.MAIN"/>
<category android:name="android.intent.category.LAUNCHER"/>
</intent-filter>
</activity>

<service android:name=".audio.music.PlaybackCaptureService" android:foregroundServiceType="mediaProjection|microphone" android:exported="false"/>
</application>
</manifest>