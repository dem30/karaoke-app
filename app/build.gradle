plugins {
    id("com.android.application")
}

android {
    namespace = "com.karaokeapp"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.karaokeapp"
        // minSdk 29 vi AudioPlaybackCaptureConfiguration (capture nhac tu app khac)
        // chi ho tro tu Android 10 (API 29) tro len.
        minSdk = 29
        targetSdk = 34
        versionCode = 1
        versionName = "0.1.0-phase1"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

dependencies {
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.core:core:1.13.1")
    // WebRTC: neu aichatvn2 dang dung thu vien nao (vd io.getstream:stream-webrtc-android,
    // hoac org.webrtc:google-webrtc), them dong tuong ung o day sau khi copy code sang.
configurations.all {
    exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
    exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
}

}
